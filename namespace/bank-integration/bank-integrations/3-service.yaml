apiVersion: v1
kind: Service
metadata:
  name: bank-integrations
  labels:
    app.kubernetes.io/name: bank-integrations
    app: bank-integrations
spec:
  type: ClusterIP
  ports:
    - name: main-port
      protocol: TCP
      port: 80
      targetPort: 8080
    - name: metrics
      protocol: TCP
      port: 9101
      targetPort: 9101
  selector:
    app.kubernetes.io/name: bank-integrations
    app: bank-integrations
